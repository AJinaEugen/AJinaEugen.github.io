var employed = false; // boolean
var age = 24; // number
var name = 'First Last'; // string

// object (array)
var skills = ['html', 'css', 'js'];
// object (json)
var person = {
  employed: true,
  age: 29
};
// object (DOM, other)
var el = document.getElementById('el');

function getMsg(name){
    return  `Hey ${name}  ! Whassap` // Backtick usage

}

